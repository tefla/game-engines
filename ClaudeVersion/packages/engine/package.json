{
  "name": "@oort/engine",
  "version": "0.1.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "build": "bun build ./src/index.ts --outdir ./dist"
  },
  "dependencies": {
    "@oort/core": "workspace:*",
    "@oort/slate": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
