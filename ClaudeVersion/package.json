{
  "name": "oort-engine",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "games/*",
    "apps/*"
  ],
  "scripts": {
    "test": "bun test",
    "build": "bun run --filter '*' build",
    "dev": "bun run --filter puzzle-demo dev"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.3.0"
  }
}
